Yo !

On vient d'avoir un-boulet qu a acheté des photos de chatons sur le site alors que c'est gratos sur le net.
Le client qui a l'adresse <%= user.email%> a payé pour ces photos :

<% @items.each do |item| %>
      <%= item.title %>:</strong><%= link_to item.image_url, image_path(item.image_url), data: {turbolinks: false}%>
<%end%>

Pognon, pognon, pognon facile ! 
Start up nation !