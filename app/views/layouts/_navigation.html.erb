<header>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <%= link_to 'Chat-perd-lipopette', root_path, class:'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <%= link_to 'Tous les items', items_path, class:'nav-link nav-item' %><span class="sr-only">(current)</span>
          </li>
      </ul>

  <% if user_signed_in? %>

    <p class="margin_right" id="notice"><%= notice %></p>
      <li class="nav-item dropdown">
        <a class="nav-link text-light dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Dropdown</a>
        <div class="dropdown-menu" x-placement="bottom-start" style="position: absolute; transform: translate3d(0px, 40px, 0px); top: 0px; left: 0px; will-change: transform;">
          <%= link_to current_user.email, user_path(current_user) %>
            <div class="dropdown-divider"></div>
          <%= link_to 'Deconnexion', destroy_user_session_path, method: :delete, class:'dropdown-item' %>
        </div>
      </li>

  <% else %>
          <%= link_to 'Connexion', new_user_session_path, class:'nav-link text-light' %>
          <%= link_to 'Inscription', new_user_registration_path, class: 'nav-link text-light' %>

  <% end %>
        </div>
  </nav>
</header>
